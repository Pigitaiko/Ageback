{
  "name": "x402-cashback-protocol",
  "version": "1.0.0",
  "description": "X402 Cashback Protocol - Hardened Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:hoodi": "hardhat run scripts/deploy.js --network taikoHoodi",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "test": "hardhat test"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6"
  }
}
